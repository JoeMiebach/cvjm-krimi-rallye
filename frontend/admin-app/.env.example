########################################
# admin-app/.env.example
# Kopieren nach .env (lokal) bzw. .env.production (Build fürs Live-Backend)
########################################

VITE_API_BASE_URL=http://localhost:8080/api

# Für Produktions-Build gegen das Live-Backend (in .env.production setzen):
# VITE_API_BASE_URL=https://cvjm.joe-miebach.de/api

# Platzhalter, bis die Rallye-Auswahl (Dropdown) im Admin-UI implementiert ist.
# Anders als in der Team-App (dort bestimmt der Startcode die Rallye) muss der
# Admin hier zwischen mehreren Rallyes wählen können -- offener Punkt, siehe
# Erläuterung im Chat.
VITE_DEFAULT_RALLYE_ID=1
